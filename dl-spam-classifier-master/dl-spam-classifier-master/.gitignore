.venv/
__pycache__/
.ipynb_checkpoints/
*.pyc
.idea/
*.docx
Assignment 1 guide.ipynb

# GloVe file - too large for GitHub (1GB)
glove.twitter.27B.100d*.txt

# Assignment PDF - not needed in repo
*.pdf

# Datasets and raw training data - not needed once model is trained
spam.csv
training_data/

# Large / internal model artifacts (indices not needed for inference)
outputs/train_idx.npy
outputs/test_idx.npy
# tokenizer.json is committed — needed by the deployed inference API

# Windows
Thumbs.db
desktop.ini
